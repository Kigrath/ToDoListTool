# Workspace Instructions

- This project is a local ToDo tool built with Node.js and Express.
- The UI lives in `public/` and talks to the JSON-backed API in `server.js`.
- Persistent data is stored in `data/todos.json`.
- Keep task and subtask shapes compatible with the current API contract.
- Prefer small, focused changes that preserve the local-first workflow.
